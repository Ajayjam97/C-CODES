#include <iostream>

using namespace std;

int main()
{
    int main = 0;
    wchar_t wchar = 'A';
    //wchar_t is a new keyord for Widechar datatype.
    //char could only represnt 256 values of ASCII butr wchar 65536 Unicode values
    //Also in this program using, namespace are newly added keywords other than int which was a c keyword
    cout<<main<<endl;
    //Main despite of being a keyword can be manipulated because it is a pre-defined identifier keyword.
}